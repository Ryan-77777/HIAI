---
description: 每次完成代码修改后自动提交并推送到 GitHub，保持 GitHub 和 Vercel 始终是最新版本
alwaysApply: true
---

# 自动 Git 提交 & 推送

每次完成任何代码修改、文件更新后，**必须自动执行以下步骤**，无需用户额外要求：

## 执行步骤

```bash
cd "/Users/bytedance/Library/Mobile Documents/com~apple~CloudDocs/smart-summary"
git add .
git commit -m "<本次改动的简短描述>"
git push
```

## Commit 消息规范

| 类型 | 场景 |
|------|------|
| `feat:` | 新增功能或组件 |
| `fix:` | 修复 bug 或样式问题 |
| `style:` | 纯样式 / 动效调整 |
| `docs:` | 文档、Design Skill、README 更新 |
| `token:` | Token 参数变更 |
| `refactor:` | 代码重构，功能不变 |

## 重要说明

- 推送成功后，Vercel 会在 1-2 分钟内自动重新部署到 `hiaidesign.vercel.app`
- 推送完成后，在回复末尾告知用户：`已自动推送到 GitHub，Vercel 将在 1-2 分钟内更新。`
- 如果 git push 失败，立即报告错误原因，不要静默跳过
